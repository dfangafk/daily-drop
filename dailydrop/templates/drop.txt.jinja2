Daily Drop — {{ date }}
=====================================================

{{ all_finds | length }} new find{{ "s" if all_finds | length != 1 else "" }} today

=====================================================
NEW FINDS
=====================================================
{% for find in all_finds %}
[{{ loop.index }}] {{ find.title }}
    Source: {{ find.source_name }}{% if find.published_at %} | {{ find.published_at.strftime('%d %b') }}{% endif %}

    {{ find.url }}
{% if find.description %}
    {{ find.description }}
{% endif %}
-----------------------------------------------------
{% endfor %}
