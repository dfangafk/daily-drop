# Email
SENDER_EMAIL=you@example.com
SMTP_PASSWORD=
RECEIVER_EMAIL=

# SMTP server (defaults to Gmail; override for other providers)
# NOTIFY__SMTP_HOST=smtp.gmail.com
# NOTIFY__SMTP_PORT=465

# Pipeline tuning
PIPELINE__LOG_LEVEL=INFO
