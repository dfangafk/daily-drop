feedcurator — {{ date }}
=====================================================

SUMMARY ({{ all_items | length }} new item{{ "s" if all_items | length != 1 else "" }} today)
-----------------------------------------------------
{{ summary if summary else "No summary available." }}

{% if picks %}
=====================================================
RECOMMENDED FOR YOU
=====================================================
{% for pick in picks %}
[{{ loop.index }}] {{ pick.item.title }}
    Source: {{ pick.item.source_name }}
    {{ pick.rationale }}
    {{ pick.item.url }}

{% endfor %}
{% endif %}
=====================================================
ALL NEW ITEMS
=====================================================
{% for item in all_items %}
[{{ loop.index }}] {{ item.title }}
    Source: {{ item.source_name }}{% if item.published_at_display %} | {{ item.published_at_display }}{% endif %}

    {{ item.url }}

-----------------------------------------------------
{% endfor %}
